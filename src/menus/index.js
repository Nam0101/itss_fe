import Authentication from "./Authentication";
import Admin from "./Admin";

const MenuItems = {
  authentication: {
    item: Authentication,
  },
  admin: {
    item: Admin,
  },
};

export default MenuItems;
